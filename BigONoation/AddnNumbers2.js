function addUpTo(number) {
    return number * (number + 1) / 2;
};

console.log(addUpTo(3));

//Here, number of operations are always 3 no matter the input.
//Therefore, time complexity of Big O notation is O(1).